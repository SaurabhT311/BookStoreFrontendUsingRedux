const baseUrl=`http://localhost:4000`;

export const LOGIN_API = {
    LOGIN_API_URL: `${baseUrl}/login`
}

export const REGISTRATION_API = {
    REGISTRATION_API_URL: `${baseUrl}/registration`
}